const axios = require('axios');
(async () => {
  try {
     const token = "YOUR_TOKEN"; // Wait, I don't have the token. I should just check if the PUT endpoint exists. But we already got 401.
  } catch(e){}
})();
